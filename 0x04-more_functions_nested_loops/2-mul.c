#include<stdio.h>
#include"main.h"

/**
 * mul - its for return 0 if its a lower case
 *
 * @a: check the input of the function
 *
 * @b: input for the function
 *
 * Return: 1 (success)
 * 0 (not success)
*/

int mul(int a, int b)
{
	return (a * b);
}

