#include<stdio.h>
#include"main.h"

/**
 * _isalpha - its for return 0 if its a lower case
 *
 * @c: check the input of the function
 *
 * Return: 1 (success)
 * 0 (not success)
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	return (0);
}

