#include<stdio.h>

/**
 * main - entry point
 *
 * discreption: its a c program for printing integer from 0 to 9
 *
 * Return: 0 (success)
*/

	int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
	printf("%d", i);
	}
	return (0);
}
